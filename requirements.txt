# Core Django
Django==6.0.6
django-environ==0.13.0

# Database (PostgreSQL 18+ obligatorio)
# psycopg v3 es el driver moderno y mantenido; reemplaza a psycopg2.
# Django >= 4.2 lo detecta automáticamente con ENGINE='django.db.backends.postgresql'.
psycopg[binary]==3.3.4

# Auth
django-allauth==65.18.0

# API
djangorestframework==3.17.1

# UI / forms
django-crispy-forms==2.6
crispy-tailwind==1.0.3
django-htmx==1.27.0

# CORS / security
django-cors-headers==4.9.0

# HTTP client (integraciones MercadoLibre, Zoho Mail, WhatsApp)
requests==2.34.2

# Static files
whitenoise==6.12.0

# Production server
gunicorn==26.0.0
