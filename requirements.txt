# Core Django
Django>=4.2,<5.0

# HTMX para interactividad frontend sin JavaScript
django-htmx>=1.17.0

# Formularios con TailwindCSS
crispy-tailwind>=1.0.0
django-crispy-forms>=2.1

# Archivos estáticos en producción
whitenoise>=6.6.0

# Servidor WSGI para producción
gunicorn>=21.2.0

# Variables de entorno desde .env
django-environ>=0.11.0

# Base de datos PostgreSQL (opcional, usa SQLite por defecto)
psycopg2-binary>=2.9.9

# API REST
djangorestframework>=3.14.0

# CORS para API REST
django-cors-headers>=4.3.0

# Autenticación y registro de usuarios (django-allauth)
django-allauth==65.3.0

# Integración Zoho Mail para envío de emails
django-zoho-zeptomail==1.0.0

# Librerías útiles adicionales
Pillow>=10.0.0  # Manejo de imágenes (si se usa ImageField)
requests>=2.31.0  # Peticiones HTTP (para integraciones externas)
